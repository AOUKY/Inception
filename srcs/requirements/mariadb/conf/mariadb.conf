[mysqld]              # This section is for the MariaDB server daemon itself
bind-address=0.0.0.0  # Listen on all network interfaces, not just localhost
port=3306             # Default MySQL/MariaDB port (make sure it matches your setup)
